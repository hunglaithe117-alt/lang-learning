{
    "name": "@lang-learning/shared",
    "version": "0.0.1",
    "private": true,
    "description": "Shared types, constants, and utilities",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc --build",
        "type-check": "tsc --noEmit",
        "lint": "eslint \"src/**/*.ts\" --fix",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "typescript": "^5.8.0"
    }
}